# Core FastAPI dependencies - ONLY packages actually used in codebase
fastapi==0.115.6
uvicorn==0.32.1
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# Database - Required for SQLModel and PostgreSQL
sqlmodel==0.0.22
psycopg[binary]==3.2.3

# Authentication & Security - Used in dependencies.py and crud/user.py
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Starlette - Used in settings.py (comes with FastAPI but explicit for settings)
starlette==0.36.3

# AI Service - Only if langchain service is actually used
# langchain==0.3.12
# langchain-google-genai==2.0.5

# REMOVED UNUSED PACKAGES:
# - aiokafka (not imported anywhere)
# - emails (not imported anywhere) 
# - gunicorn (not needed for Vercel)
# - tenacity (not imported anywhere)
# - jinja2 (not imported anywhere)
# - alembic (not imported anywhere)
# - httpx (not imported anywhere)
# - bcrypt (redundant with passlib[bcrypt])
# - sentry-sdk (not imported anywhere)
# - python-multipart (not imported anywhere)
# - email-validator (not imported anywhere)
